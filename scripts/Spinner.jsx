var React = require('react');

var Spinner = React.createClass({
    render: function () {
        return <div className="sk-spinner sk-spinner-rotating-plane"></div>
    }
});

module.exports = Spinner;