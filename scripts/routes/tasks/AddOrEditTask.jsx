var React = require('react'),
    router = require('react-router');

var AddOrEditTask = React.createClass({
    mixins: [router.State],
    render: function () {
        return <div></div>
    }
});

module.exports = AddOrEditTask;