/**
 * The only reason paths are seperated out into this module is so that paths can be
 * used to match against routes when listening to the location object which only returns
 * path and not the name of the route...
 * @module paths
 */

module.exports = {
    tasks: '/tasks'
};